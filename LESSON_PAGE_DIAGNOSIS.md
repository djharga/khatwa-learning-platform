# 🔍 تشخيص مشاكل صفحة الدرس - Lesson Page Diagnosis

## 📋 Block A: Diagnosis (التشخيص)

### 1️⃣ مشاكل التخطيط (Layout Issues)

#### أ) انهيار الأعمدة نحو الوسط
**السبب:**
- استخدام `flex-1` على `UnitSidebar` و`FileList` بدون تحديد عرض ثابت للأعمدة الجانبية
- عدم وجود `flex-shrink: 0` على الأعمدة الثابتة
- عدم استخدام `min-width: 0` على العمود المرن لمنع انكماشه المفرط

**الكود المشكل:**
```tsx
<div className="flex-1 flex" style={{ minHeight: 'calc(100vh - 5rem)' }}>
  <ModuleSidebar /> {/* لا يوجد عرض ثابت */}
  {selectedModuleId && <UnitSidebar />} {/* flex-1 بدون min-width: 0 */}
  {selectedUnitId && <FileList />} {/* max-w-2xl يسبب مشاكل */}
</div>
```

#### ب) عدم محاذاة الأعمدة اليسرى واليمنى
**السبب:**
- في RTL، يجب أن يكون العمود الأيسر (Modules) له `border-left` وليس `border-right`
- الأعمدة تستخدم `border-l` الذي في RTL يكون على اليمين، مما يسبب عدم محاذاة

**الكود المشكل:**
```tsx
<div className="border-l border-neutral-200" /> {/* في RTL هذا على اليمين! */}
```

#### ج) المحتوى عالق في الوسط بدلاً من العرض الكامل
**السبب:**
- استخدام `flex-1 flex` بدون `width: 100%`
- الأعمدة الشرطية (`{selectedModuleId && ...}`) تسبب تخطيط ديناميكي
- لا يوجد `overflow: hidden` على الحاوية الرئيسية

**الكود المشكل:**
```tsx
<div className="flex-1 flex" style={{ minHeight: 'calc(100vh - 5rem)' }}>
  {/* لا يوجد width: 100% */}
  {/* الأعمدة الشرطية تكسر التخطيط */}
</div>
```

#### د) معالجة RTL غير صحيحة في Flex
**السبب:**
- استخدام `border-l` بدلاً من `border-inline-start` أو منطق RTL
- عدم استخدام `flex-row-reverse` للحفاظ على الترتيب الصحيح
- الأعمدة الشرطية تغير الترتيب البصري

**الكود المشكل:**
```tsx
<div className="flex-1 flex" dir="rtl">
  {/* في RTL، الترتيب يكون: [Modules] [Units] [Files] */}
  {/* لكن الكود الحالي يعرض: [Modules] ثم يضيف [Units] و[Files] شرطياً */}
  {/* هذا يسبب انزياح بصري */}
</div>
```

#### هـ) عمود المشاهد لا يتمدد بشكل طبيعي
**السبب:**
- `UnitSidebar` يستخدم `flex-1` لكنه شرطي (`{selectedModuleId && ...}`)
- عندما لا يكون محدداً، لا يوجد عمود وسط، فتنهار الأعمدة
- لا يوجد عمود وسط ثابت فارغ للاحتفاظ بالمساحة

**الكود المشكل:**
```tsx
{selectedModuleId && <UnitSidebar />} {/* مشروط - يختفي ويسبب انهيار */}
```

### 2️⃣ مشاكل التسلسل الهرمي البصري (Visual Hierarchy)

#### أ) الأقسام لا تبدو أكاديمية أو احترافية
**السبب:**
- استخدام ألوان `neutral-*` بدلاً من ألوان أكاديمية (أزرق/بنفسجي)
- عدم وجود ظلال (`shadow-*`) أو ارتفاع بصري
- استخدام `rounded-xl` بسيط بدلاً من تصميم محدث

#### ب) نقص المسافات النظيفة والتباين والظلال
**السبب:**
- استخدام `p-4` و`space-y-3` بدون نظام متسق
- عدم وجود ظلال طبقة (`shadow-sm`, `shadow-md`, `shadow-lg`)
- استخدام `border-2` بدون تمييز واضح

#### ج) المكونات تبدو قديمة
**السبب:**
- عدم استخدام تدرجات لونية حديثة
- عدم وجود حالات hover محسّنة
- انتقالات بطيئة (`duration-200` بدلاً من `duration-250`)

### 3️⃣ مشاكل وظيفية في الواجهة

#### أ) رسوم متحركة Accordion ضعيفة
**السبب:**
- عدم استخدام `AnimatePresence` من framer-motion بشكل صحيح
- انتقالات مباشرة بدون easing سلس

#### ب) التمييز الضعيف للوحدة المحددة
**السبب:**
- استخدام `bg-primary-50` خفيف جداً
- عدم وجود border بارز أو ظل
- لون النص لا يتغير بشكل واضح

#### ج) قسم الملفات غير قابل للقراءة
**السبب:**
- استخدام `max-w-2xl` يسبب مشاكل في العرض
- نقص المسافات الداخلية
- نص صغير جداً (`text-sm`)

### 4️⃣ نقص اللمسات النهائية في UX

#### أ) الصفحة تبدو فارغة عند عدم وجود ملفات
**السبب:**
- عدم وجود empty state مصمم بشكل جيد
- رسالة نصية بسيطة فقط

#### ب) هيكل الهيكل العظمي للتحميل يحتاج تحسين
**السبب:**
- استخدام `animate-pulse` بسيط
- عدم محاكاة التصميم النهائي بشكل دقيق

---

## ✅ الحل المطلوب

### التخطيط النهائي:
```
| LEFT (Modules) – fixed 300px |
| CENTER (Viewer/Units) – Flex grow |
| RIGHT (Files) – fixed 330px |
```

### المتطلبات:
1. ✅ عرض ثابت 300px للعمود الأيسر (Modules)
2. ✅ عرض ثابت 330px للعمود الأيمن (Files)
3. ✅ العمود الأوسط يتمدد (`flex: 1`) وله `min-width: 0`
4. ✅ RTL صحيح باستخدام `flex-row-reverse` أو `border-inline-start/end`
5. ✅ تصميم أكاديمي حديث بألوان #5B36E8 (أزرق/بنفسجي)
6. ✅ ظلال نظيفة ومسافات متسقة
7. ✅ انتقالات سلسة 0.25s
8. ✅ Empty states محسّنة
9. ✅ Skeleton loaders محسّنة

